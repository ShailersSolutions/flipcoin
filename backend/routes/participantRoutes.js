// const express = require("express");
// const router = express.Router();
// const controller = require("../controllers/participantController");
// const { authMiddleware } = require("../middlewares/authMiddleware");

// // GET all participants for a round
// router.get("/round/:roundId", authMiddleware, controller.getParticipantsByRound);

// // GET all rounds participated by a user
// router.get("/user/:userId", authMiddleware, controller.getRoundsByUser);

// // GET user participation stats
// router.get("/stats/:userId", authMiddleware, controller.getParticipantStats);

// module.exports = router;
